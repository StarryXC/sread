> Thinking

```
IO 流
序列化
文件

Java NIO
Buffer
Channel


Java IO

Commons IO
org.apache.commons:commons-io:1.3.2
```

> Memory

### Java

```
InputStream <-- Closeable
    FileInputStream
    FilterInputStream
        BufferedInputStream
        DataInputStream <-- DataInput
        PushbackInputStream
    ByteArrayInputStream
    SequenceInputStream
    ObjectInputStream <-- ObjectInput ObjectStreamConstants

DataInput
    ObjectInput <-- AutoCloseable
DataOutput
    ObjectOutput <-- AutoCloseable

OutputStream <-- Closeable Flushable
    FileOutputStream
    FilterOutputStream
        BufferedOutputStream
        DataOutputStream <-- DataOutput
        PrintStream <-- Appendable Closeable
    ByteArrayOutputStream
    ObjectOutputStream <-- ObjectOutput ObjectStreamConstants

Closeable
    close
Flushable
    flush

InputStream
    read

OutputStream
    write

File <-- Serializable Comparable
    lastModified
    setLastModified
    getAbsolutePath
    getAbsoluteFile
    isAbsolute
    getParentFile
    getParent
    getCanonicalFile
    getPath
    exists
    isFile
    isDirectory
    mkdir
    mkdirs
    list FilenameFilter
        accept
    listFiles FileFilter
        accept
    getFreeSpace
    getTotalSpace
    getUsableSpace
    createNewFile
    canRead
    canWrite
    canExecute
    isHidden
    length

FileDescriptor

Reader <-- Readable Closeable
    BufferedReader
        LineNumberReader
    CharArrayReader
    InputStreamReader
        FileReader
    FilterReader
        PushbackReader
    PipedReader
    StringReader

Writer <-- Appendable Closeable Flushable
    BufferedWriter
    CharArrayWriter
    OutputStreamWriter
        FileWriter
    FilterWriter
    PipedWriter
    PrintWriter
    StringWriter

Writer
    write

BufferedReader
    readLine

Buffer
    ByteBuffer <-- Comparable
        MappedByteBuffer
            DirectByteBuffer <-- DirectBuffer
        HeapByteBuffer
    ShortBuffer <-- Comparable
        HeapShortBuffer
    IntBuffer <-- Comparable
        HeapIntBuffer
    LongBuffer <-- Comparable
        HeapLongBuffer
    FloatBuffer <-- Comparable
        HeapFloatBuffer
    DoubleBuffer <-- Comparable
        HeapDoubleBuffer
    CharBuffer <-- Comparable Appendable CharSequence Readable
        HeapCharBuffer

Closeable
    Channel
        InterruptibleChannel
        ReadableByteChannel
            ScatteringByteChannel
        WritableByteChannel
            ByteChannel <-- ReadableByteChannel
                SeekableByteChannel
            GatheringByteChannel



AbstractInterruptibleChannel <-- Channel InterruptibleChannel
    FileChannel <-- SeekableByteChannel GatheringByteChannel ScatteringByteChannel
    DatagramChannel
    SocketChannel

FileChannel
    open
    transferFrom


```

#### Okio

```
https://square.github.io/okio/

implementation 'com.squareup.okio:okio:2.1.0'
```



### Kotlin

```

```

### Groovy

```
File
file.text
file.write(fileText,"UTF-8")
```

```

```

