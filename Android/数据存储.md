> Thinking

```
SharedPreferences
SQLite
    SQLiteOpenHelper
    SQLiteDatabase
文件
    Context
    Environment
```

> Memory

```
SharedPreferences sharedPreferences = context.getSharedPreferences("", Context.MODE_PRIVATE);
sharedPreferences = PreferenceManager.getDefaultSharedPreferences(context);

context.getCacheDir();
context.getFilesDir();
context.getDataDir();
context.getDatabasePath("");
context.getDir("", Context.MODE_PRIVATE);

Environment.isExternalStorageRemovable();
switch (Environment.getExternalStorageState()) {
    case Environment.MEDIA_MOUNTED:
}

class AskBrainSQLiteOpenHelper extends SQLiteOpenHelper {
    public AskBrainSQLiteOpenHelper(@Nullable Context context) {
        super(context, "name", null, 1);
    }
    @Override
    public void onCreate(SQLiteDatabase sqLiteDatabase) { }
    @Override
    public void onUpgrade(SQLiteDatabase sqLiteDatabase, int i, int i1) { }
    @Override
    public void onOpen(SQLiteDatabase db) { super.onOpen(db); }
    @Override
    public void onConfigure(SQLiteDatabase db) { super.onConfigure(db); }
}
SQLiteOpenHelper sqLiteOpenHelper = new AskBrainSQLiteOpenHelper(context);
sqLiteOpenHelper.getDatabaseName();
sqLiteOpenHelper.getReadableDatabase();
SQLiteDatabase sqLiteDatabase = sqLiteOpenHelper.getWritableDatabase(); // 基于文件
sqLiteDatabase.execSQL("");
sqLiteDatabase.delete("", "", new String[1]);
sqLiteDatabase.insert("", "", new ContentValues());
sqLiteDatabase.beginTransaction(); // 事物整个数据库文件加锁
sqLiteDatabase.setTransactionSuccessful();
sqLiteDatabase.endTransaction();
SQLiteDatabase.create(new SQLiteDatabase.CursorFactory() { // 基于内存
    @Override
    public Cursor newCursor(SQLiteDatabase sqLiteDatabase, SQLiteCursorDriver sqLiteCursorDriver, String s, SQLiteQuery sqLiteQuery) { return null; }
});

Cursor cursor = sqLiteDatabase.query("", new String[1], "", new String[1], "", "", "");
cursor.getCount(); cursor.getColumnCount();
cursor.getColumnIndex(""); cursor.getColumnIndexOrThrow("");
cursor.moveToFirst(); cursor.moveToLast();
cursor.moveToPrevious(); cursor.moveToNext();
cursor.moveToPosition(1);
while (cursor.moveToNext()) {
    int columnIndex = cursor.getColumnIndex("");
    String string = cursor.getString(columnIndex);
}
cursor.close();
```

