> Thinking

```
AnimationUtils
    Animation
    LayoutAnimationController
    Interpolator
AnimatorInflater
    Animator
    StateListAnimator

LayoutInflater

TypedArray
TypedValue

AssetManager
Resources
ResourcesCompat

AttributeSet
```

> Memory

```
String string = context.getString(R.string.app_name);
CharSequence text = context.getText(R.string.app_name);
context.getDrawable(R.drawable.ic_launcher_background);
ContextCompat.getDrawable(context, R.drawable.shape);
context.getColor(R.color.black);
ContextCompat.getColor(context, R.color.black);

Resources resources = context.getResources();
int identifier = resources.getIdentifier("activity_main", "layout", getPackageName());
resources.getDrawable(R.drawable.ic_launcher_background);
resources.getString(R.string.app_name);
resources.getColor(R.color.black);
resources.getResourceName(R.string.app_name);
resources.getResourcePackageName(R.string.app_name);
resources.getResourceTypeName(R.string.app_name);
resources.getResourceEntryName(R.string.app_name);
ResourcesCompat.getColor(getResources(), R.color.black, getTheme());
ResourcesCompat.getFont(getResources(), R.font.font);
ResourcesCompat.getDrawable(getResources(), R.drawable.shape, getTheme());

AssetFileDescriptor assetFileDescriptor = resources.openRawResourceFd(R.raw.beep);
assetFileDescriptor.getFileDescriptor();
assetFileDescriptor.getStartOffset();
assetFileDescriptor.getLength();
assetFileDescriptor.close();

Configuration configuration = resources.getConfiguration();
boolean isOrientation = configuration.orientation == Configuration.ORIENTATION_LANDSCAPE;
configuration.setToDefaults();

DisplayMetrics displayMetrics = resources.getDisplayMetrics();
displayMetrics.density;
displayMetrics.densityDpi;
displayMetrics.scaledDensity;
displayMetrics.widthPixels;
displayMetrics.heightPixels;
displayMetrics.xdpi;
displayMetrics.ydpi;

AssetManager assetManager = context.getAssets(); // assets/
assetManager.open("");
assetManager.openFd("");
assetManager.list("");

TypedArray typedArray = context.obtainStyledAttributes(new int[1]);
typedArray.getFloat(0, 0f);
typedArray.getColor(0, Color.BLUE);
typedArray.getIndexCount();
typedArray.recycle();

TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_DIP, 0, new DisplayMetrics());

LayoutInflater layoutInflater = LayoutInflater.from(context);
layoutInflater = (LayoutInflater) context.getSystemService(Context.LAYOUT_INFLATER_SERVICE);
layoutInflater = activity.getLayoutInflater();
View view = layoutInflater.inflate(R.layout.activity_main, new LinearLayout(context), true);

Animation animation = AnimationUtils.loadAnimation(context, R.anim.set);
LayoutAnimationController layoutAnimationController = AnimationUtils.loadLayoutAnimation(context, R.anim.set);
Interpolator interpolator = AnimationUtils.loadInterpolator(context, R.anim.la);

Animator animator = AnimatorInflater.loadAnimator(context, R.animator.animator);
StateListAnimator stateListAnimator = AnimatorInflater.loadStateListAnimator(context, R.animator.animator);

attributeSet.getAttributeCount();
attributeSet.getAttributeName(0);
attributeSet.getAttributeValue(0);


:资源访问
<package>.R.<type>.<name> # 代码访问
@[package:]<type>/<name> # XML 访问
	layout res/layout layout.xml
	drawable res/drawable drawable.xml
	color res/color color.xml
	anim res/anim anim.xml
	animator res/animator animator.xml 动画资源
	interpolator res/anim interpolator.xml
	menu res/menu menu.xml 菜单资源
	raw
	xml
	font res/font font.xml 字体资源
	res/values integer bool dimen color string style id
		array # int[] string[] array
		Quantity Strings (Plurals)
:资源适配目录
mipmap drawable
	-mdpi -hdpi -xhdpi -xxhdpi -xxxhdpi
	-anydpi-v26
	-v24 -v26
raw
layout
	-sw550dp-large
values
	-large -xlarge
	-en -zh-rTW
	-w820dp -sw600dp -sw720dp-land
	-v21
	-night
```

```
public static int dp2px(Context context, float dp) {
    float scale = context.getResources().getDisplayMetrics().density;
    return (int) (dp * scale + 0.5F);
}

public static int px2dp(Context context, float px) {
    float scale = context.getResources().getDisplayMetrics().density;
    return (int) (px / scale + 0.5F);
}
```

