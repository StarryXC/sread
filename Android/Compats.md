> Thinking

```
ContextCompat
ActivityCompat
```

> Memory

```
ContextCompat
	getColor
	checkSelfPermission
ActivityCompat
	requestPermissions
ContextCompat.getColor(
ContextCompat.getDrawable(
```

