> Thinking

```
包装类
大数值
精确计算
浮点计算
```

> Memory

### Java

```
Integer.toHexString(100);
Integer.toBinaryString(100);
Integer.toOctalString(100);
Integer.valueOf(100);
Integer.parseInt("100");

Float.valueOf("1");

Character.isWhitespace('a');
Character.isDigit('a');


BigDecimal decimalA = new BigDecimal("0");
BigDecimal decimalB = new BigDecimal("0");
decimalA.add(decimalB);
decimalA.subtract(decimalB);
decimalA.multiply(decimalB);
decimalA.divide(decimalB, 2, BigDecimal.ROUND_DOWN);
decimalA.setScale(2, BigDecimal.ROUND_DOWN);
decimalA.max(decimalB);
decimalA.min(decimalB);
decimalA.compareTo(decimalB);
decimalA.toPlainString();
decimalA.floatValue();
```

### Groovy

```
包装类
BigInteger = 30g BigDecimal = 3.5g
```

