> Thinking

```

```

> Memory

### Java

```
Class
AccessibleObject
Array

Byte.class byte.class
Short.class short.class
method.getName();
Integer.class int.class
Long.class long.class
Float.class float.class
Double.class double.class
Character.class char.class
Boolean.class boolean.class

InputStream inputStream = ClassNote.class.getResourceAsStream("/packet.xml");
method.setAccessible(true);

Object object = new Object();
Class clazz = Class.forName("");
clazz.getName();
clazz.getSimpleName();
clazz.getCanonicalName();
clazz.isInstance(object);
clazz.isEnum();

clazz.getConstructor();

Method method = clazz.getMethod("");
method = clazz.getDeclaredMethod("");
Method methods[] = clazz.getMethods();
methods = clazz.getDeclaredMethods();
Object invoke = method.invoke(object, "");
method.getName();

Field field = clazz.getField("");
field = clazz.getDeclaredField("");
Field fields[] = clazz.getFields();
fields = clazz.getDeclaredFields();
field.getName();
field.set();

Annotation annotation = clazz.getAnnotation(clazz);
annotation = clazz.getDeclaredAnnotation(clazz);
Annotation annotations[] = clazz.getAnnotations();
annotations = clazz.getDeclaredAnnotations();

Object object = new Object();
Array.getLength(object);
Array.get(object, 1);
Array.set(object, 1, object);
```



