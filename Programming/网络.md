> Thinking

```
TCP
UDP
HTTP

WebSockets
https://github.com/TooTallNate/Java-WebSocket
'org.java-websocket:Java-WebSocket:1.3.6'
```

> Memory

### Java

```
TCP Socket
UDP Socket
Http URL
URLDecoder URLEncoder

InetAddress.getLocalHost().toString();

// http 写入流程
URL url = new URL("");
int port = url.getPort() == -1 ? 80 : url.getPort();
String requestLine = "POST " + url.getPath() + " HTTP/1.1\r\n";
Socket socket = new Socket(InetAddress.getByName(url.getHost()), port);
OutputStream outStream = socket.getOutputStream();
outStream.write(requestLine.getBytes());
BufferedReader reader = new BufferedReader(new InputStreamReader(socket.getInputStream()));
if (reader.readLine().indexOf("200") == -1) {
}
outStream.flush();
outStream.close();
socket.close();

URLDecoder.decode("", "");
URLEncoder.encode("", "");

URI
URI uri = URI.create("");
uri.getAuthority();
uri.getHost();
uri.getPort();
uri.getQuery();
uri.getPath();
uri.getScheme();
uri.getUserInfo();

HostnameVerifier hostnameVerifier = new HostnameVerifier() {
    @Override
    public boolean verify(String s, SSLSession sslSession) { return true; }
};
HttpsURLConnection.setDefaultHostnameVerifier(hostnameVerifier);

Proxy.getDefaultHost();
Proxy.getDefaultPort();
InetSocketAddress inetSocketAddress = new InetSocketAddress("", 80);
java.net.Proxy proxy = new java.net.Proxy(java.net.Proxy.Type.HTTP, inetSocketAddress);

URL url = new URL("");
url.getAuthority();
url.getAuthority();
url.getHost();
url.getProtocol();
url.getPort(); url.getDefaultPort();
url.getQuery();
url.getUserInfo();
url.getFile();
url.getPath();

URLConnection urlConnection = url.openConnection(proxy);
urlConnection.getConnectTimeout(); urlConnection.setConnectTimeout(1);
urlConnection.getReadTimeout(); urlConnection.setReadTimeout(1);
urlConnection.getDoOutput(); urlConnection.setDoOutput(true);
urlConnection.getDoInput(); urlConnection.setDoInput(true);
urlConnection.addRequestProperty("", "");
urlConnection.connect();
urlConnection.getInputStream();
urlConnection.getOutputStream();
urlConnection.getHeaderFields();
urlConnection.getHeaderField("");

HttpURLConnection httpURLConnection = (HttpURLConnection) urlConnection;
httpURLConnection.getResponseCode();
httpURLConnection.getRequestMethod(); httpURLConnection.setRequestMethod("GET");
httpURLConnection.getResponseMessage();
httpURLConnection.getInstanceFollowRedirects(); httpURLConnection.setInstanceFollowRedirects(true);
httpURLConnection.disconnect();



```

### Struts

```
Action
DispatchAction
ActionMapping
ActionForward
ActionForm
FormFile

<struts-config>
<form-beans>
<form-bean>
<action-mappings>
<action>
<forward>
```

### Struts 2

```
com.opensymphony.xwork2
Action
ActionSupport
ActionContext
ServletActionContext
RequestAware
SessionAware
ApplicationAware

<struts>
<package>
<action>
<result>
<constant>
<include>
```

### Spring

```
@SpringBootApplication
@Configuration
@Bean

SpringApplication

<beans>
<bean>
<property>
<value>
<ref>

ApplicationContext
ClassPathXmlApplicationContext
```

