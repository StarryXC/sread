> Thinking

```

```

> Memory

```
异步处理/线程通信

Handler
Looper
Message
MessageQueue

Parcelable
    Message

Handler
    obtainMessage
    sendEmptyMessage
    sendEmptyMessageAtTime
    sendEmptyMessageDelayed
    sendMessage
    post
    postDelayed
    quitSynchronously

Looper
    myLooper
    prepare
    loop
    myQueue

Message
    obtain
    sendToTarget
    recycle MAX_POOL_SIZE 50

AsyncTask
Thread
    HandlerThread
IntentService
JobService

AsyncTask
    onPreExecute
    doInBackground
    onPostExecute
    onProgressUpdate
    onCancelled
    execute
    executeOnExecutor AsyncTask.THREAD_POOL_EXECUTOR Build.VERSION.SDK_INT >= 11
    cancel
    isCancelled

HandlerThread
    start
    getLooper

JobParameters
JobInfo.Builder # (jobId, jobService) ComponentName(context.getPackageName(), JobService.class.getName())
	setPeriodic # 执行间隔时间
	setRequiredNetworkType # 执行网络类型 JobInfo.NETWORK_TYPE_NONE
	setMinimumLatency # 任务运行最少延迟时间
	setRequiresCharging # 是否充电时执行
	build # JobInfo 获取服务
JobScheduler # getSystemService Context.JOB_SCHEDULER_SERVICE
	schedule # JobInfo
JobService
	permission="android.permission.BIND_JOB_SERVICE"
	exported="true"
```

#### LoaderManager

```

LoaderManager
    initLoader LoaderCallbacks
        onCreateLoader
        onLoadFinished
        


if (id == LOADER_ALL) {
                CursorLoader cursorLoader = new CursorLoader(getActivity(),
                        MediaStore.Images.Media.EXTERNAL_CONTENT_URI, IMAGE_PROJECTION,
                        null, null, IMAGE_PROJECTION[2] + " DESC");
                return cursorLoader;
            } else if (id == LOADER_CATEGORY) {
                CursorLoader cursorLoader = new CursorLoader(getActivity(),
                        MediaStore.Images.Media.EXTERNAL_CONTENT_URI, IMAGE_PROJECTION,
                        IMAGE_PROJECTION[0] + " like '%" + args.getString("path") + "%'", null, IMAGE_PROJECTION[2] + " DESC");
                return cursorLoader;
            }

```



#### WorkManager

```

```

### HTML5

```
worker
```

